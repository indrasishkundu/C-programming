#include<stdio.h>
#include<conio.h>
#include<string.h>

int main()
{
	char str[100],ch;
	int v=0,l,i;
	printf("\n Enter the string 1-->");
	gets(str);
	l=strlen(str);
	for(i=0;i<l;i++)
	{
		ch=str[i]
		if(ch=='A')||(ch=='a')||(ch=='E')||(ch=='e')||(ch=='I')||(ch='i')||(ch='O')||(ch=='o')||(ch=='U')||(ch=='u')||
	}
	{
	
	v++;
}
printf("\n total no of vowel of the text...%d");
getch();
return 0;
}
